Create users (and be automatically assigned as an admin if they are the first user).

Assign roles (such as manager) to users, subject to certain conditions (like the admin’s manager limit).

Delete users (except themselves).

Update user details, including name, email, and role.

Increase manager limit, which allows them to create more managers.

Soft delete users, effectively hiding them from active use, but not removing them from the database.

1. View Users
   Admin can view all users in the system.

2. Search/Filter Users
   Admin can search and filter users by parameters like name, email, etc.

3. View Manager Details
   Admin can view details about managers and their teams.

4. Assign Specific Permissions to Roles
   Admin can assign specific permissions to roles.

5. Audit Log
   Admin can view audit logs of activities performed by users in the system.

6. System Settings and Configuration
   Admin can change system settings.

7. Reset User Password
   Admin can reset a user's password.

8. Bulk Actions (Delete/Assign Roles)
   Admin can delete multiple users or assign roles in bulk.

9. Email/Notification Management
   Admin can send notifications or emails to users.

10. Backup/Restore Data
    Admin can manage backups of data.

11. System Health and Statistics
    Admin can check system performance and statistics.

12. Ban or Block User
    Admin can block or ban users from the system.

13. Customizable User Roles
    Admin can create and manage custom roles with specific permissions.

14. Set User Expiration/Timeout
    Admin can set a user's account expiration.
